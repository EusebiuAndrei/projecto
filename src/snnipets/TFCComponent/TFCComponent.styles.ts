import { createStyles, makeStyles, Theme } from '@material-ui/core/styles';

interface TFCComponentStylesProps {}

const useStyles = makeStyles<Theme, TFCComponentStylesProps>((theme) =>
    createStyles({
    }),
);

export default useStyles;
